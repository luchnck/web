server {
		listen	80 default_server;
		location ^~ /uploads/ {
				root /home/box/web;
				return 200;	
			}
		location ~* ^.*\.+$ {
				root 	/home/box/web/public;
				return 200;
			}
		location ~* ^[^\.]$ {
				return 404;
			}
	} 
